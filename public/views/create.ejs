<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('layout/entryheader.ejs') %> 
    <script defer src="/scripts/create.min.js?v=7"></script>
</head>
<body>
    <div class="container">
        <form class="form" method="post" action="/chat" onsubmit="return check();">
            <div class="subform" id="form1">
                <div class="title">
                    <div>Create chat</div>
                    <img src="/images/pokeball.png" height="40px" width="40px" alt="logo">
                </div>
                <div class="form-field clickable">
                    <label for="key" id="label">Tap to copy <i id="lb__icon" class="fa-regular fa-clone"></i> <span class="remainingTime">02:00</span> </label>
                    <div id="keyErr" class="errLog"></div>
                    <input autocomplete="off" type="text" name="key" class="copy noSelect" id="key" placeholder="xxx-xxx-xxx-xxx" readonly value="<%=key_%>">
                </div>
                <div class="form-field clickable">
                    <button id="next" type="button">Next</button>
                </div>
                <div class="redirect">
                    <fieldset>
                        <legend>Or</legend>
                        <a id="redirect" href="/join" class="noSelect clickable" onclick="return wait();">Join chat</a>
                    </fieldset>
                </div>
            </div>
            <div class="subform" id="form2">
                <div class="form-field">
                    <label for="username">Choose username <i class="fa-solid fa-id-card-clip"></i> <span class="remainingTime">02:00</span></label>
                    <div id="usernameErr" class="errLog"></div>
                    <input autocomplete="off" type="text" name="username" id="username" placeholder="Username">
                </div>
                <div class="form-field avatar-field">
                    <label for="avatar">Choose avatar <i class="fa-solid fa-user-astronaut"></i></label>
                    <div id="avatarErr" class="errLog"></div>
                    <div class="avatar-container">
                        <%- include ('layout/avatars.ejs') %>
                    </div>
                </div>
                <div class="form-field range">
                    <label for="max-user">Select max user (<span id="rangeValue">2</span>)</label>
                    <input type="range" class="clickable" id="maxuser" name="maxuser" max="10" min="2" value="2">
                </div>
                <div class="form-field">
                    <button id="enter" class="clickable" type="submit">Enter Chat</button>
                </div>
            </div>
        </>
    </div>
    <fieldset class="howtouse line"> 
        <legend class="h1">How to use</legend>
        <ul>
            <li>Copy <b>Key</b> to send to your friends. </li>
            <li>You can anyways copy that later.</li>
            <li>Create an <b>username</b> and choose <b>avatar.</b> </li>
            <li><b>Username</b> cannot contain Space and special charecters.</li>
            <li>Chats will be deleted after you disconnect or exit</li>
            <li>We don't have any database. So, your data won't be stored anywhere.</li>
            <li>Messages are end-to-end encrypted.</li>
        </ul>
    </fieldset>
    <%- include ('layout/entryfooter.ejs') %> 
</body>
</html>